%h1 Listing vacantes
.row
  - @vacantes.each do |vacante|
    .col-sm-4
      .panel.panel-primary
        .panel-heading
          %h4.panel-title
          = Cargo.find(vacante.cargo_id).nombre
        .panel-body.form-horizontal
          .col-sm-6
            = vacante.fecha_vencimiento
        .panel-footer
          .actions
            = link_to edit_vacante_path(vacante) do
              %i.fa.fa-pencil.fa-2x.text-primary
            = link_to vacante, :method => :delete, :data => { :confirm => 'Are you sure?' } do
              %i.fa.fa-times.fa-2x.text-danger
= link_to 'New Vacante', new_vacante_path
