= form_for @departamento do |f|
  - if @departamento.errors.any?
    .row
      .col-sm-6
        .alert.alert-danger
          %strong= "Departamento no puede guardarse. #{pluralize(@departamento.errors.count, "error")}"
          %ul
            - @departamento.errors.full_messages.each do |message|
              %li= message
  .row
    .col-md-6
      .panel.panel-primary
        .panel-heading
          %h4.panel-title
            Datos
        .panel-body.form-horizontal
          .field.form-group
            =f.label :codigo, :class=> "control-label col-sm-2"
            .col-sm-6
              = f.text_field :codigo, :class=> "form-control input-sm upper"
          .field.form-group
            = f.label :nombre, :class=> "control-label col-sm-2"
            .col-sm-8
              = f.text_field :nombre, :class=> "form-control input-sm"
          .field.form-group
            .col-sm-2
            .field.checkbox.col-sm-10
              = f.label :activo
              = f.check_box :activo
        .panel-footer
          .actions
            = f.button(type: "submit", class: "btn btn-sm btn-primary") do
              Guardar
              %i.fa.fa-save
