<%= form_for(@centro_costo) do |f| %>
  <% if @centro_costo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@centro_costo.errors.count, "error") %> prohibited this centro_costo from being saved:</h2>

      <ul>
      <% @centro_costo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br>
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :activo %><br>
    <%= f.check_box :activo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
